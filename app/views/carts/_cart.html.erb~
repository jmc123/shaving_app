
<h4>Your Cart</h4>
  <table>
  <%= render(cart.line_items) %>
  <tr class="total_line">
    <td colspan="2">Total</td>
    <td class="total_cell"><%= number_to_currency(cart.total_price, unit: "&euro;") %></td>
  </tr>
  </table>
  <p></p>
  <%= button_to 'Checkout', cart,  class: "btn btn-primary" %>

  <%= button_to 'Empty Cart', cart, remote: true, method: :delete, class: "btn btn-danger", confirm: "Are you sure you want to destroy your cart?" %>

