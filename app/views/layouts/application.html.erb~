<!DOCTYPE html>
<html>
<head>
  <title>Shaving Products</title>
  <%= stylesheet_link_tag    "application", media: "all","data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %><!-- <label id="code.jlt"/> -->
  <%= csrf_meta_tags %><!-- <label id="code.csrf"/> -->
</head>
<body class="<%= controller.controller_name %>">
  <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <nav>
            <ul class="nav pull-left">
              <li><%= link_to raw("<i class=\"icon-home icon-white\"></i> Home"), store_path %></li>
              <% if user_signed_in? %>
              <li class="text-info">Signed in as <%= current_user.email %></li> 
              <li><%= link_to raw("<i class=\"icon-user icon-white\"></i> Sign-Out"), destroy_user_session_path, method: :delete %> </li>
              <% else %>
              <li><%= link_to raw("<i class=\"icon-user icon-white\"></i> Sign-In"), new_user_session_path %></li>
              <li><%= link_to raw("<i class=\"icon-user icon-white\"></i> Sign-Up"), new_user_registration_path %></li>
              <% end %>
            </ul>
	
		<form class="navbar-search pull-right">
              <div class="input-append">	    
		 <input type="text" class="search-query" placeholder="Search">
	      </div>
	        </form>
	    
          </nav>
        </div>
      </div>
    </header>
    <div class="container-fluid span12">
  <p class="alert-error"><%= alert  %></p>
  <p class="alert-success"><%= notice %></p>
      <div class="row-fluid">



	<div class="span9">
           
          <%= yield %><!-- <label id="code.depot.e.include"/> -->
	</div>
	<div class="span3">
          <div id="cart">
          <% if @cart %>
            <%= hidden_div_if(@cart.line_items.empty?, id: 'cart') do %>
              <%= render @cart %>
            <% end %>
	    <% end %>
          </div>
        </div>
      </div>
    </div>

   

  

</body>

</html>
